<%= form_tag(search_user_path, :method => "get",   
    class: 'navbar-form navbar-left') do %>  
  <div class="input-group">  
    <%= search_field_tag :search, params[:search], placeholder: "Search", class: "form-control" %>  
    <div class="input-group-btn">  
      <%= button_tag "Click to search", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>  
    </div>  
  </div>  
<% end %>

<% if !@results.blank? %>
    <% @results.each do |result| %>
        <a href="/users/<%= result.id %>">
        <%= result.username %><br>
        </a>  
    <% end %>
<% end %>
==============
<section id="view-searched-users">
  <form id="search-user">
    <input id="search-user-name" type="search" class="form-control mr-sm-2" placeholder="Type username">
    <button type="submit" class="btn btn-outline-success my-2 my-sm-0" >Search</button>
  </form>
  <ul id="searched-user-list"></ul>
</section>

<script type="text/template" name="searched-user-item-tmpl">
{{= username}}
Hello
</script>
==============